<% provide(:title,'Foo') %>
<!DOCTYPE html>
<html>
<head>
	<title>VideoLibUi | <%=yield(:title) %></title>
	<%= csrf_meta_tags %>

	<%= stylesheet_link_tag     %>
	<%= javascript_include_tag %>
</head>

<body>

<% flash.each do |key, value| %>
  <%= content_tag :div, value, class: "flash #{key}" %>
<% end %>
	
	<%= render 'layouts/header'%>
	<div class="container">
		<%=yield%>
		<%= render 'layouts/footer' %>
	</div>

</body>
</html>
